/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AccionSpFormularioService } from './AccionSpFormulario.service';

describe('Service: AccionSpFormulario', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AccionSpFormularioService]
    });
  });

  it('should ...', inject([AccionSpFormularioService], (service: AccionSpFormularioService) => {
    expect(service).toBeTruthy();
  }));
});
